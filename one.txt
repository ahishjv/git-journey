one
11111111

git add --all
git add -A

These add everything in the project

git add .

This adds everything in the current directory

git add * 

This adds everything that is new or modified but deleted will not get staged


Local --
  Working directory
  Staging Area
  Commit to local repo


git reset --hard 
  brings back all files deleted as well in the working directory

git rm file 
  remove files and stages the removed file

git rm --cached file 
  removed file only from staging , keeps in the working directory

git rm --force file 
  removes file from working diorectory

git rm -r folder
  removes all files within the folder


Branching

git branch -- lists all branches
git branch name --- creates a branch with name 

git checkout name --- goes to name branch

git merge branchname -m "message"

Remote --
    gh auth 
    set contents permission on the PAT

    git push -u origin main
    git config --list
     
